<<<<<=======================Python Implementation==================>>>>>>

Install the requirements
------------------------
=> pip install pycryptodomex

Running the app:
----------------
=> Run App.py
=> First Select Receive option from the Gui to Receive the data. (Acts as the server) (Use the IP Address of the receiver system)
=> Then Select Send option with Receiver IP Address.