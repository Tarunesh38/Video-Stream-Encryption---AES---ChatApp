To Run program install these dependencies using pip

pip install pycryptodomex


----------------------------------------------------------------------
1) As Server Client Setup, run two instances of the GUI.

2) The Reciever needs to run first before transmitting

3) Bind the Reciever socket to the Reciever IP and a port

4) For Transmitted give the ip and port of the reciever's socket in tranmitter side
----------------------------------------------------------------------
This is in assumption that they are in the same network